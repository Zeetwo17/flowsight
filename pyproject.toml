[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flowsight"
version = "0.1.0"
description = "Digital twin for supply chain ripple-effect mitigation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
